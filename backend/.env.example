# Server
PORT=3000

# MongoDB (optional for demo — in-memory data is used if not set)
MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/gatekeeper

# Redis (optional for demo)
REDIS_USERNAME=default
REDIS_PASSWORD=yourpassword
REDIS_HOST=redis-host.example.com
REDIS_PORT=6379
